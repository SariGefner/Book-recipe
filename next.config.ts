import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ['infobase.click'], // Add the external hostname here
  },
};

export default nextConfig;
// module.exports = {
//   images: {
//     domains: ['infobase.click'], // Add the external hostname here
//   },
// };
